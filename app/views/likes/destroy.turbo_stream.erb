<%
  partial = if params[:source] == 'tweet_show'
              'tweets/show_stats'
            else
              'tweets/tweet'
            end
%>
<%= turbo_stream.replace dom_id(@tweet) do %>
  <%= render partial, tweet_presenter: TweetPresenter.new(@tweet.reload, current_user: current_user.reload) %>
<% end %>